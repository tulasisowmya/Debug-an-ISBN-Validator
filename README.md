# Debug-an-ISBN-Validator
A Python ISBN Validator that checks ISBN‑10 and ISBN‑13 codes. It validates input format, length, and characters, then calculates and compares the check digit using official algorithms. Outputs clear messages for valid, invalid, or malformed inputs.
